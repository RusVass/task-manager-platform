{
    "name": "lesson-9",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "type": "module",
    "scripts": {
        "dev": "nodemon server.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "format:check": "prettier --check .",
        "format:write": "prettier --write .",
        "lint:check": "eslint .",
        "lint:fix": "eslint --fix .",
        "docker:up": "docker run -d --name task-manager-api --env-file .env -p 3001:3001 task-manager-api",
        "docker:down": "docker rm -f task-manager-api",
        "docker:build": "docker build -t task-manager-api .",
        "docker:restart": "npm run docker:down && npm run docker:build && npm run docker:up",
        "docker:logs": "docker logs -f task-manager-api",
        "docker:ps": "docker ps"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "bcrypt": "^5.1.1",
        "dotenv": "^17.2.3",
        "express": "^4.18.2",
        "jsonwebtoken": "^9.0.3",
        "mongoose": "^8.0.0",
        "swagger-jsdoc": "^6.2.8",
        "swagger-ui-express": "^5.0.0"
    },
    "devDependencies": {
        "eslint": "^8.52.0",
        "eslint-config-prettier": "^9.0.0",
        "nodemon": "^3.1.11",
        "prettier": "^3.0.3"
    }
}
